-----BEGIN AGE ENCRYPTED FILE-----
PLACEHOLDER — replace this file with the real ciphertext before deploying
hello-locked. Encrypt your OIDC client secret to the repo key:

  curl -s https://<your-garnix>/api/keys/<owner>/<repo>/repo-key.public > repo.pub
  printf %s '<client secret>' | age -R repo.pub -a > hello-locked-client-secret.age

or let the authentik-provision helper write it for you.
-----END AGE ENCRYPTED FILE-----
