description: >-
  Fetching private dependencies with 'enableGithubOrgAccessTokens' enabled,
  and with dependencies which some users don't have access to, but setting
  the flag for skipping this check.
repo: "joegoldin/garnix-integration-minimal"
skipPrivateInputsCheck: true
results:
  - name: "Evaluate flake.nix"
    index: Last
    outputLocation: Github
    result: success
