{
  "name": "on-demand-resolver",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist || true && tsc --build",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^25.0.9",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "zod": "^4.3.5"
  }
}
